---
import Layout from '../layouts/Layout.astro'
import { DashboardApp } from '../components/dashboard/DashboardApp'
---

<Layout title="Breach Dashboard - Real-time Security Incident Monitoring">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <!-- Modern Dashboard Header -->
    <div class="mb-10 text-center">
      <div class="inline-flex items-center space-x-3 mb-4">
        <div class="w-16 h-16 bg-gradient-to-br from-teal to-purple rounded-2xl flex items-center justify-center shadow-glow-teal">
          <span class="text-dark-900 text-2xl">🛡️</span>
        </div>
        <div class="text-left">
          <h1 class="text-4xl font-bold bg-gradient-to-r from-teal-light via-teal to-purple-light bg-clip-text text-transparent">
            Security Intelligence Hub
          </h1>
          <p class="text-lg text-gray-400 font-medium">
            Real-time breach monitoring across 37+ sources
          </p>
        </div>
      </div>
      <p class="text-gray-400 max-w-3xl mx-auto leading-relaxed">
        Comprehensive monitoring of security incidents from government portals, state attorney general offices,
        cybersecurity news feeds, and specialized breach databases with AI-powered analysis.
      </p>
    </div>

    <!-- Modern Stats Overview -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-12">
      <!-- Breach Notifications Card -->
      <div class="group relative overflow-hidden bg-dark-800/50 backdrop-blur-sm p-6 rounded-2xl border border-dark-700/50 hover:border-teal/30 transition-all duration-300 hover:shadow-glow-teal">
        <div class="absolute inset-0 bg-gradient-to-br from-teal/5 to-teal/10 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
        <div class="relative">
          <div class="flex items-center justify-between mb-4">
            <div class="w-12 h-12 bg-gradient-to-br from-teal-light to-teal rounded-xl flex items-center justify-center shadow-md">
              <span class="text-dark-900 text-xl">📊</span>
            </div>
            <div class="w-2 h-2 bg-teal rounded-full animate-pulse"></div>
          </div>
          <div>
            <p class="text-sm font-semibold text-teal uppercase tracking-wide">Active</p>
            <h3 class="text-base font-medium text-gray-300 mt-1">Breach Notifications</h3>
            <p class="text-3xl font-bold text-white mt-2" id="total-breaches">
              <span class="animate-pulse bg-gradient-to-r from-dark-700 to-dark-600 bg-clip-text text-transparent shimmer">Loading...</span>
            </p>
            <p class="text-sm text-gray-400 mt-1" id="news-count">
              <span class="animate-pulse">+ Loading news...</span>
            </p>
          </div>
        </div>
      </div>

      <!-- People Affected Card -->
      <div class="group relative overflow-hidden bg-dark-800/50 backdrop-blur-sm p-6 rounded-2xl border border-dark-700/50 hover:border-purple/30 transition-all duration-300 hover:shadow-glow-purple">
        <div class="absolute inset-0 bg-gradient-to-br from-purple/5 to-purple/10 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
        <div class="relative">
          <div class="flex items-center justify-between mb-4">
            <div class="w-12 h-12 bg-gradient-to-br from-purple-light to-purple rounded-xl flex items-center justify-center shadow-md">
              <span class="text-dark-900 text-xl">👥</span>
            </div>
            <div class="w-2 h-2 bg-purple rounded-full animate-pulse"></div>
          </div>
          <div>
            <p class="text-sm font-semibold text-purple uppercase tracking-wide">Critical</p>
            <h3 class="text-base font-medium text-gray-300 mt-1">People Affected</h3>
            <p class="text-3xl font-bold text-white mt-2" id="total-affected">
              <span class="animate-pulse bg-gradient-to-r from-dark-700 to-dark-600 bg-clip-text text-transparent shimmer">Loading...</span>
            </p>
            <p class="text-sm text-gray-400 mt-1">
              <span id="affected-change">+120% from last week</span>
            </p>
          </div>
        </div>
      </div>

      <!-- Active Sources Card -->
      <div class="group relative overflow-hidden bg-dark-800/50 backdrop-blur-sm p-6 rounded-2xl border border-dark-700/50 hover:border-teal/30 transition-all duration-300 hover:shadow-glow-teal">
        <div class="absolute inset-0 bg-gradient-to-br from-teal/5 to-teal/10 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
        <div class="relative">
          <div class="flex items-center justify-between mb-4">
            <div class="w-12 h-12 bg-gradient-to-br from-teal-light to-teal rounded-xl flex items-center justify-center shadow-md">
              <span class="text-dark-900 text-xl">🔄</span>
            </div>
            <div class="w-2 h-2 bg-teal rounded-full animate-pulse"></div>
          </div>
          <div>
            <p class="text-sm font-semibold text-teal uppercase tracking-wide">Healthy</p>
            <h3 class="text-base font-medium text-gray-300 mt-1">Active Sources</h3>
            <p class="text-3xl font-bold text-white mt-2">37+</p>
            <p class="text-sm text-gray-400 mt-1">Government & News</p>
          </div>
        </div>
      </div>

      <!-- Last Updated Card -->
      <div class="group relative overflow-hidden bg-dark-800/50 backdrop-blur-sm p-6 rounded-2xl border border-dark-700/50 hover:border-purple/30 transition-all duration-300 hover:shadow-glow-purple">
        <div class="absolute inset-0 bg-gradient-to-br from-purple/5 to-purple/10 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
        <div class="relative">
          <div class="flex items-center justify-between mb-4">
            <div class="w-12 h-12 bg-gradient-to-br from-purple-light to-purple rounded-xl flex items-center justify-center shadow-md">
              <span class="text-dark-900 text-xl">📅</span>
            </div>
            <div class="w-2 h-2 bg-purple rounded-full animate-pulse"></div>
          </div>
          <div>
            <p class="text-sm font-semibold text-purple uppercase tracking-wide">Recent</p>
            <h3 class="text-base font-medium text-gray-300 mt-1">Last Updated</h3>
            <p class="text-3xl font-bold text-white mt-2" id="last-updated">
              <span class="animate-pulse bg-gradient-to-r from-dark-700 to-dark-600 bg-clip-text text-transparent shimmer">Loading...</span>
            </p>
            <p class="text-sm text-gray-400 mt-1">Auto-refreshing</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Main Dashboard Content -->
    <DashboardApp client:load />
  </div>
</Layout>

<script>
  import { supabase } from '../lib/supabase'
  import { formatAffectedCount } from '../lib/utils'

  // Load dashboard stats
  async function loadStats() {
    try {
      console.log('Loading dashboard statistics...')

      // Get breach count (excluding news feeds)
      const { count: totalBreaches, error: countError } = await supabase
        .from('v_breach_dashboard')
        .select('*', { count: 'exact', head: true })
        .in('source_type', ['State AG', 'Government Portal', 'Breach Database', 'State Cybersecurity', 'State Agency', 'API'])

      if (countError) {
        console.error('Error getting breach count:', countError)
        throw countError
      }

      // Get news count
      const { count: totalNews, error: newsError } = await supabase
        .from('v_breach_dashboard')
        .select('*', { count: 'exact', head: true })
        .in('source_type', ['News Feed', 'Company IR'])

      if (newsError) {
        console.error('Error getting news count:', newsError)
        throw newsError
      }

      // Get total affected individuals (sum of all non-null affected_individuals)
      const { data: affectedData, error: affectedError } = await supabase
        .from('v_breach_dashboard')
        .select('affected_individuals')
        .not('affected_individuals', 'is', null)

      if (affectedError) {
        console.error('Error getting affected individuals:', affectedError)
        throw affectedError
      }

      const totalAffected = affectedData?.reduce((sum, item) => sum + (item.affected_individuals || 0), 0) || 0

      // Get latest scraped date
      const { data: latestData, error: latestError } = await supabase
        .from('v_breach_dashboard')
        .select('scraped_at')
        .not('scraped_at', 'is', null)
        .order('scraped_at', { ascending: false })
        .limit(1)

      if (latestError) {
        console.error('Error getting latest scraped date:', latestError)
        throw latestError
      }

      const lastUpdated = latestData?.[0]?.scraped_at || new Date().toISOString()

      // Update the UI
      const totalBreachesEl = document.getElementById('total-breaches')
      const newsCountEl = document.getElementById('news-count')
      const totalAffectedEl = document.getElementById('total-affected')
      const lastUpdatedEl = document.getElementById('last-updated')

      if (totalBreachesEl) {
        totalBreachesEl.innerHTML = (totalBreaches || 0).toLocaleString()
        totalBreachesEl.classList.remove('animate-pulse', 'bg-gradient-to-r', 'from-dark-700', 'to-dark-600', 'bg-clip-text', 'text-transparent', 'shimmer')
      }

      if (newsCountEl) {
        newsCountEl.innerHTML = `+ ${(totalNews || 0).toLocaleString()} news articles`
        newsCountEl.classList.remove('animate-pulse')
      }

      if (totalAffectedEl) {
        totalAffectedEl.innerHTML = formatAffectedCount(totalAffected)
        totalAffectedEl.classList.remove('animate-pulse', 'bg-gradient-to-r', 'from-dark-700', 'to-dark-600', 'bg-clip-text', 'text-transparent', 'shimmer')
      }

      if (lastUpdatedEl) {
        lastUpdatedEl.innerHTML = new Date(lastUpdated).toLocaleDateString()
        lastUpdatedEl.classList.remove('animate-pulse', 'bg-gradient-to-r', 'from-dark-700', 'to-dark-600', 'bg-clip-text', 'text-transparent', 'shimmer')
      }

      console.log('Dashboard stats loaded:', {
        totalBreaches,
        totalNews,
        totalAffected,
        lastUpdated
      })

    } catch (error) {
      console.error('Failed to load stats:', error)

      // Show error state
      const errorMessages = {
        'total-breaches': 'Error loading',
        'news-count': 'Error loading',
        'total-affected': 'Error loading',
        'last-updated': 'Error loading'
      }

      Object.entries(errorMessages).forEach(([id, message]) => {
        const el = document.getElementById(id)
        if (el) {
          el.innerHTML = message
          el.className = 'text-red-500'
        }
      })
    }
  }

  // Load stats after a short delay to ensure Supabase is initialized
  setTimeout(loadStats, 100)
</script>
