---
import Layout from '../layouts/Layout.astro'
---

<Layout title="Breach Dashboard - Real-time Security Incident Monitoring">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <!-- Dark Theme Header -->
    <div class="mb-8 text-center">
      <div class="inline-flex items-center space-x-3 mb-6">
        <div class="w-12 h-12 bg-blue-600 rounded-lg flex items-center justify-center">
          <span class="text-white text-xl">🛡️</span>
        </div>
        <div class="text-left">
          <h1 class="text-3xl font-bold text-white">
            Security Intelligence Hub
          </h1>
          <p class="text-slate-400 font-medium">
            Real-time breach monitoring across 37+ sources
          </p>
        </div>
      </div>
      <p class="text-slate-400 max-w-3xl mx-auto leading-relaxed">
        Comprehensive monitoring of security incidents from government portals, state attorney general offices,
        cybersecurity news feeds, and specialized breach databases with AI-powered analysis.
      </p>
    </div>

    <!-- Dark Theme Stats Cards -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
      <div class="bg-slate-800/50 backdrop-blur-sm p-6 rounded-lg border border-slate-700/50 hover:bg-slate-800/70 transition-all duration-200">
        <div class="flex items-center justify-between mb-4">
          <div class="w-8 h-8 bg-blue-600 rounded flex items-center justify-center">
            <span class="text-white text-sm">📊</span>
          </div>
          <div class="text-xs text-blue-400 font-medium px-2 py-1 bg-blue-900/30 rounded">
            Active
          </div>
        </div>
        <div>
          <p class="text-xs font-medium text-slate-400 uppercase tracking-wide mb-1">Breach Notifications</p>
          <p class="text-2xl font-bold text-white" id="total-breaches">
            <span class="animate-pulse text-slate-500">Loading...</span>
          </p>
          <p class="text-xs text-slate-500 mt-1" id="news-count">
            <span class="animate-pulse">+ Loading news...</span>
          </p>
        </div>
      </div>

      <div class="bg-slate-800/50 backdrop-blur-sm p-6 rounded-lg border border-slate-700/50 hover:bg-slate-800/70 transition-all duration-200">
        <div class="flex items-center justify-between mb-4">
          <div class="w-8 h-8 bg-red-600 rounded flex items-center justify-center">
            <span class="text-white text-sm">👥</span>
          </div>
          <div class="text-xs text-red-400 font-medium px-2 py-1 bg-red-900/30 rounded">
            Critical
          </div>
        </div>
        <div>
          <p class="text-xs font-medium text-slate-400 uppercase tracking-wide mb-1">People Affected</p>
          <p class="text-2xl font-bold text-white" id="total-affected">
            <span class="animate-pulse text-slate-500">Loading...</span>
          </p>
          <p class="text-xs text-slate-500 mt-1">+120% from last week</p>
        </div>
      </div>

      <div class="bg-slate-800/50 backdrop-blur-sm p-6 rounded-lg border border-slate-700/50 hover:bg-slate-800/70 transition-all duration-200">
        <div class="flex items-center justify-between mb-4">
          <div class="w-8 h-8 bg-green-600 rounded flex items-center justify-center">
            <span class="text-white text-sm">🔄</span>
          </div>
          <div class="text-xs text-green-400 font-medium px-2 py-1 bg-green-900/30 rounded">
            Healthy
          </div>
        </div>
        <div>
          <p class="text-xs font-medium text-slate-400 uppercase tracking-wide mb-1">Active Sources</p>
          <p class="text-2xl font-bold text-white">37+</p>
          <p class="text-xs text-slate-500 mt-1">Government & News</p>
        </div>
      </div>

      <div class="bg-slate-800/50 backdrop-blur-sm p-6 rounded-lg border border-slate-700/50 hover:bg-slate-800/70 transition-all duration-200">
        <div class="flex items-center justify-between mb-4">
          <div class="w-8 h-8 bg-orange-600 rounded flex items-center justify-center">
            <span class="text-white text-sm">📅</span>
          </div>
          <div class="text-xs text-orange-400 font-medium px-2 py-1 bg-orange-900/30 rounded">
            Recent
          </div>
        </div>
        <div>
          <p class="text-xs font-medium text-slate-400 uppercase tracking-wide mb-1">Last Updated</p>
          <p class="text-2xl font-bold text-white" id="last-updated">
            <span class="animate-pulse text-slate-500">Loading...</span>
          </p>
          <p class="text-xs text-slate-500 mt-1">Auto-refreshing</p>
        </div>
      </div>
    </div>

    <!-- Main Dashboard Content -->
    <div id="dashboard-root"></div>
  </div>
</Layout>

<script>
  import { DashboardApp } from '../components/dashboard/DashboardApp'
  import { createRoot } from 'react-dom/client'
  import React from 'react'
  import { supabase } from '../lib/supabase'
  import { formatAffectedCount } from '../lib/utils'

  // Mount React app
  const container = document.getElementById('dashboard-root')
  if (container) {
    const root = createRoot(container)
    root.render(React.createElement(DashboardApp))
  }

  // Load dashboard stats
  async function loadStats() {
    try {
      console.log('Loading dashboard statistics...')

      // Get breach count (excluding news feeds)
      const { count: totalBreaches, error: countError } = await supabase
        .from('v_breach_dashboard')
        .select('*', { count: 'exact', head: true })
        .in('source_type', ['State AG', 'Government Portal', 'Breach Database', 'State Cybersecurity', 'State Agency', 'API'])

      if (countError) {
        console.error('Error getting breach count:', countError)
        throw countError
      }

      // Get news count
      const { count: totalNews, error: newsError } = await supabase
        .from('v_breach_dashboard')
        .select('*', { count: 'exact', head: true })
        .in('source_type', ['News Feed', 'Company IR'])

      if (newsError) {
        console.error('Error getting news count:', newsError)
        throw newsError
      }

      // Get total affected individuals (sum of all non-null affected_individuals)
      const { data: affectedData, error: affectedError } = await supabase
        .from('v_breach_dashboard')
        .select('affected_individuals')
        .not('affected_individuals', 'is', null)

      if (affectedError) {
        console.error('Error getting affected individuals:', affectedError)
        throw affectedError
      }

      const totalAffected = affectedData?.reduce((sum, item) => sum + (item.affected_individuals || 0), 0) || 0

      // Get latest scraped date
      const { data: latestData, error: latestError } = await supabase
        .from('v_breach_dashboard')
        .select('scraped_at')
        .not('scraped_at', 'is', null)
        .order('scraped_at', { ascending: false })
        .limit(1)

      if (latestError) {
        console.error('Error getting latest scraped date:', latestError)
        throw latestError
      }

      const lastUpdated = latestData?.[0]?.scraped_at || new Date().toISOString()

      // Update the UI
      const totalBreachesEl = document.getElementById('total-breaches')
      const newsCountEl = document.getElementById('news-count')
      const totalAffectedEl = document.getElementById('total-affected')
      const lastUpdatedEl = document.getElementById('last-updated')

      if (totalBreachesEl) {
        totalBreachesEl.innerHTML = (totalBreaches || 0).toLocaleString()
      }

      if (newsCountEl) {
        newsCountEl.innerHTML = `+ ${(totalNews || 0).toLocaleString()} news articles`
      }

      if (totalAffectedEl) {
        totalAffectedEl.innerHTML = formatAffectedCount(totalAffected)
      }

      if (lastUpdatedEl) {
        lastUpdatedEl.innerHTML = new Date(lastUpdated).toLocaleDateString()
      }

      console.log('Dashboard stats loaded:', {
        totalBreaches,
        totalNews,
        totalAffected,
        lastUpdated
      })

    } catch (error) {
      console.error('Failed to load stats:', error)

      // Show error state
      const errorMessages = {
        'total-breaches': 'Error loading',
        'news-count': 'Error loading',
        'total-affected': 'Error loading',
        'last-updated': 'Error loading'
      }

      Object.entries(errorMessages).forEach(([id, message]) => {
        const el = document.getElementById(id)
        if (el) {
          el.innerHTML = message
          el.className = el.className.replace('text-gray-900 dark:text-white', 'text-red-500 dark:text-red-400')
        }
      })
    }
  }

  // Load stats after a short delay to ensure Supabase is initialized
  setTimeout(loadStats, 100)
</script>
